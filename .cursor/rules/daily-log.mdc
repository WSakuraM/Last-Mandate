---
description: 每次改仓库必须更新当日 DAILY_LOG（单文档按日合并）
alwaysApply: true
---

# 每日工作日志

仓库只有一份日志：[`docs/DAILY_LOG.md`](docs/DAILY_LOG.md)。

## 必须做

1. 本对话若对项目有任何文件增删改（含文档、数据、代码、规则），在收尾前更新 `docs/DAILY_LOG.md`。
2. 用**当天日期**作二级标题：`## YYYY-MM-DD`（以用户环境「今天」为准）。
3. **同一天只有一节**：已有当天标题则在该节内增删改；禁止同一天再开第二段。
4. 文字要短、清楚：用小标题归类（如「剧本」「系统」「引擎」「Git」）；写重点与影响，避免无意义的文件路径清单。
5. 当天节末可用一两句「当前进度」收束。

## 不要

- 不要为每天新建其它日志文件。
- 不要把同一次会话拆成多个同日期标题。
- 不要只写「更新了文件」而不说改了什么意义。
