# 《末命》设计留痕 Archive

本目录存放**可追溯、可迭代**的设计与计划资产。日常可改写的工作稿仍在 [docs/](../docs/)](index.md)](CHANGELOG.md) 写一条（中文，面向「这版改了什么」）。  
5. 若是不可逆重大决策，在 `decisions/` 加一条 ADR。

Agent 规则见 [`.cursor/rules/design-traceability.mdc`](.cursor/rules/design-traceability.mdc)。

## 每日备份

**已取消。** 不再使用 Windows 计划任务 `LastMandate-DailyBackup-1600`。  
请靠收工时手动双推（GitHub + Gitee）；需要时可自行运行 `scripts/daily_backup.ps1`，但项目默认不注册定时任务。
