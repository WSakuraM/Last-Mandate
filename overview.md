# 区块四 落地概览——跨幕回忆回收机制

## 完成内容

按设计敲定记录区块四（跨幕回忆回收），全部待落地项落地完成。

### 修改文件（6 个）

| 文件 | 变更 |
|------|------|
| `IssueManager.gd` | 新增 `draw_montage(8)` Ⅲ类严格优先填满 + `draw_blood_edict_faces(3)` 血诏脸谱 + `draw_memory_texts` 改调 `draw_montage` 兼容 |
| `HUD.gd` | 气数条改"天命" + 颜色梯度（暗金→橙红→深红）+ 锁底4标记 + ≥70显示"你救不了这座江山" |
| `MeishanDirector.gd` | 重写为四阶段流程：雪夜独白→血诏脸谱→8条回忆蒙太奇→渐黑终幕 |
| `Act1Closure.gd` | 改用 `draw_montage(8)` 展示8条回忆 + 按支柱着色 |
| `13_设计敲定记录.md` | 区块四落地状态全部更新为已落地 |
| `12_剧情走向总图.md` | 蒙太奇抽取描述更新为"抽8条·Ⅲ类严格优先" + 气数锁底UI标注 |

### 蒙太奇抽取逻辑

```
IssueManager.draw_montage(count=8)
  ├─ Ⅲ类（人民疾苦）严格优先填满 count 个名额
  │   按权重降序排列，有多少填多少
  ├─ 名额未满则补 Ⅰ/Ⅱ 类
  │   同样按权重降序排列
  └─ 返回完整 memory dict 数组（含 id/weight/text/pillar）

IssueManager.draw_blood_edict_faces(count=3)
  └─ 返回权重最高的 2-3 条 Ⅲ 类回忆
     作为血诏「勿伤我百姓」时浮现的具体脸
```

### 气数明示但锁底

| 气数范围 | 颜色 | 状态 |
|----------|------|------|
| <30 | 暗金 (0.7, 0.6, 0.35) | 天命尚存 |
| 30-70 | 橙红 (0.8, 0.4, 0.2) | 衰势渐显 |
| ≥70 | 深红 (0.6, 0.15, 0.12) | 气数将尽 + "你救不了这座江山" |

永久标记：`▎锁底 4 · 不可清零`（气数永不低于 4.0，与 AGENTS.md 一致）

### 煤山终章四阶段

```
Phase 1 雪夜独白
  煤山·夜雪 → 诸臣误朕 → 勿伤我百姓

Phase 2 血诏脸谱（如有Ⅲ类回忆）
  "——你想起了他们——"
  draw_blood_edict_faces(3) 逐一浮现幽灵卡片
  按支柱着色：Ⅲ暖红 / Ⅱ冷灰 / Ⅰ暗金

Phase 3 回忆蒙太奇（如有回忆）
  "——一幕的回忆，终章的回声——"
  draw_montage(8) 按序展现，每条2.5秒
  E跳过 / ESC退出

Phase 4 渐黑 + 终幕
  "1644 · 崇祯十七年 · 煤山"
```

### 支柱着色

| 支柱 | 含义 | 颜色 |
|------|------|------|
| Ⅲ | 人民疾苦 | 暖红 (0.85, 0.6, 0.5) |
| Ⅱ | 朝堂国事 | 冷灰 (0.6, 0.65, 0.75) |
| Ⅰ | 信王个人 | 暗金 (0.82, 0.78, 0.6) |

## 校验

- Godot 4.7.2 headless 校验：0 报错
- 本地提交：`caf07a0`（7 files, +265/-80 行）
- 推送：沙箱限制，需用户在已登录终端 `git push origin main`

## 总体进度

| 区块 | 状态 |
|------|------|
| 区块一：主线脊柱 M1A0~M1A5 | ✅ 全部落地 |
| 区块二：玩法循环比重与节奏 | ✅ 全部落地 |
| 区块三：支线清单与串联 | ✅ 第一幕 portion 全部落地（6/12，余待 M2/M3） |
| 区块四：跨幕回忆回收 | ✅ **全部落地** |

**四个区块全部落地完成。** 后续可推进方向：
1. 补第二幕朝堂/终章煤山建模
2. 换 AI 精模替换占位灰盒
3. 按敲定记录继续写第二幕代码
4. 调试运行完整第一幕流程
